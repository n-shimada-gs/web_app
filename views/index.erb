<h1>メモアプリ</h1>

<p>作成したメモを一覧で表示します</p>
<p>
  <a class='button-link' href='/top/new'>新しいメモを追加</a>
</p>

<div class='memo_list'>
  <ul>
    <% @memos.each do |memo| %>
      <li><a href='/top/<%= h(memo['id']) %>'><%= h(memo['title']) %></a></li>
    <% end %>
  </ul>
</div>
